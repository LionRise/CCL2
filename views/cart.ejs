<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('./partials/stylePlugin'); %>
    <title>Server Side Coding</title>
</head>

<body>
<header ><%- include('./partials/navBar'); %></header>
<main style="padding-top: 70px;" class="d-flex flex-column align-items-center justify-content-center mt-0">
    <h2><u>Your Cart</u></h2>
    
    <!-- Products -->
    <div style="width: 70%" class="container my-1">
        <div class="row">
            <% for(let i = 0; i < cartItems.length; i++) { %>
                <div class="col-xl-3 col-lg-4 col-md-4 col-sm-6 d-flex">
                    <a href="/products/<%= cartItems[i].fk_product_id %>">
                        <div class="card w-100 my-2 shadow-2-strong">
                            <img class="card-img-top" style="aspect-ratio: 1 / 1"
                                 alt="<%= cartItems[i].title %>"
                                 src="../../uploads/<%= cartItems[i].productPicName %>">
                            <div class="card-body d-flex flex-column">
                                <h5 class="card-title"><%= cartItems[i].title %></h5>
                                <p class="card-text"><%= cartItems[i].price %>€</p>
                                <div class="card-footer d-flex justify-content-between justify-content-between align-items-end px-0 pb-0 mt-auto">
                                    <a class="btn btn-light border px-2 pt-2">
                                        <i class="bi bi-heart"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                    </a>
                </div>
            <% } %>
        </div>
    </div>
</main>

</body>
</html>